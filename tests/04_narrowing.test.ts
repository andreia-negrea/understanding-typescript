// import { test, expect, expectTypeOf } from 'vitest';
// import { normalizeId } from '../src/exercises/01_tsc-basics-and-basic-types/04_narrowing';

// test('signature is (string | number) => string', () => {
//   expectTypeOf(normalizeId).parameters.toEqualTypeOf<[string | number]>();
//   expectTypeOf(normalizeId).returns.toEqualTypeOf<string>();
// });

// test('runtime behavior', () => {
//   expect(normalizeId('abc')).toBe('ABC');
//   expect(normalizeId(123)).toBe('123');
// });
