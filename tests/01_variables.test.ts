// import { expect, test, expectTypeOf } from 'vitest';
// import * as mod from '../src/exercises/01_tsc-basics-and-basic-types/01_variables';

// test('age is a number', () => {
//   expectTypeOf(mod.age).toEqualTypeOf<number>();
//   expect(typeof mod.age).toBe('number');  
// });

// test('active is a boolean', () => {
//   expectTypeOf(mod.active).toEqualTypeOf<boolean>();
//    expect(typeof mod.active).toBe('boolean'); 
// });

// test('tags is string[]', () => {
//   expectTypeOf(mod.tags).toEqualTypeOf<string[]>();
//   mod.tags.forEach((t) => expect(typeof t).toBe('string'));
// });
