/**
 * GOAL:
 *  - Create three arrays with correct element types.
 *  - `names` → string[]
 *  - `ages` → number[]
 *  - `activeFlags` → boolean[]
 */

export const names = ['Ana', 42, true]; //  should be string[]
export const ages = [25, '30', false]; // should be number[]
export const activeFlags = [true, 'yes']; // should be boolean[]

/**
 * GOAL:
 *  - `scores` should be an array of numbers using Array<number> syntax.
 *  - `userTuple` should be a tuple [string, number].
 */

export const scores = ['10', '20']; 

export const userTuple = [30, 'Ana']; 
