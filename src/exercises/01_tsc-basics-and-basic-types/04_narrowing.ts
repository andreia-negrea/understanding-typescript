/**
 * TODO:
 *  - Accept a string | number
 *  - If it's a string, return it uppercased
 *  - If it's a number, return it as a string unchanged
 *  - Use `typeof` for narrowing (no other concepts)
 */

// export function normalizeId(id: string | number) {
//   // this fails for numbers because toUpperCase exists only on strings
//   return id.toUpperCase();
// }