/**
 * GOAL:
 *  - Create a function `getUserInfo` that returns a tuple [string, number].
 *  - The first element is the user's name, the second is their age.
 */

export function getUserInfo() {
  // missing types, returning wrong order
  return [25, 'Ana'];
}