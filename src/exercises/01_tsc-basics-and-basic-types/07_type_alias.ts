/**
 * GOAL:
 *  - Create a type alias `Person` for:
 *    { name: string; age: number }
 *  - Create a constant `student` of type Person.
 */

export const student = {
  name: 'Alex',
  age: '22', 
};
