/**
 * TODO:
 *  - Make `age` a number (not string)
 *  - Make `active` a boolean (not number)
 *  - Make `tags` a string array
 */

export let age = '25'; 
export let active = 1; 
export let tags = ['ts', 42]; 
