/**
 * GOAL:
 *  - Create a type `Status` that can only be:
 *    'loading' | 'success' | 'error'
 *  - Create a variable `currentStatus` of type Status.
 */

export let currentStatus = 'done'; // not part of Status
