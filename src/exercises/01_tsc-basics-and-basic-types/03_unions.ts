/**
  * TODO:
 *  - Allow the function to accept either a string or a number.
 *  - The function should return a string: "ID-" followed by the value.
 *  - You don't need to add any runtime checks or conversions for now.
 */

// export function formatId(id) {
//   return 'ID-' + id; // works only when correctly typed
// }